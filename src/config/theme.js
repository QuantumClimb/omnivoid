/**
 * Global Theme Configuration
 * Centralizes color scheme for easy updates across shows
 */

const THEME = {
  // Primary accent color - changes per show
  PRIMARY_COLOR: '#FF0000', // Current show: Red (previously was #99ccff - Blue)
  
  // Text colors (unchanged)
  TEXT_PRIMARY: '#FFFFFF',
  TEXT_SECONDARY: '#99ccff',
  
  // Component-specific colors
  ACCENT: '#FF0000',
  BORDER: '#FF0000',
  GLOW: '#FF0000'
};

// Export for use in modules
export default THEME;
